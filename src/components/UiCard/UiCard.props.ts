export type UiCardProps = {
   /**Defines if card is hoverable or not
    * @default false
    */
   hoverable?: boolean

   /**
    * Size of the card
    * @default "md"
    * @type "sm" | "md" | "lg"
    */
   size?: "sm" | "md" | "lg"
}